package com.gig.entity;

import java.sql.Timestamp;

import com.fasterxml.jackson.annotation.JsonFormat;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
//@Entity
//@Table(name="user")

public class User {

	/*
	 * @Id private String userId;
	 * 
	 * @Column(name = "first_name", nullable = false) private String firstName;
	 * 
	 * @Column(name = "middle_name", nullable = false) private String middleName;
	 * 
	 * @Column(name = "last_name", nullable = false) private String lastName;
	 * 
	 * @Column(name = "mobile", nullable = false) private String mobile;
	 * 
	 * @Column(name = "email", nullable = false) private String email;
	 * 
	 * @Column(name = "status", nullable = false) private String status;
	 * 
	 * @JsonFormat(shape = JsonFormat.Shape.STRING, pattern =
	 * "yyyy-MM-dd HH:mm:ss.S") private Timestamp createdAt;
	 * 
	 * @JsonFormat(shape = JsonFormat.Shape.STRING, pattern =
	 * "yyyy-MM-dd HH:mm:ss.S") private Timestamp lastUpdatedAt;
	 */
}
